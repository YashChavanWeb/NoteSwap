// utils.js

export const downloadNote = (noteUrl) => {
    // Simulate downloading by opening a new tab with the note URL
    window.open(noteUrl, '_blank');
    // Alternatively, you can fetch the file and handle it here
};
